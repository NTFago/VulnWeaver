# VulnWeaver 大作业要求验收矩阵与演示交付说明

> 本文档由总控（astra）维护，对照《大作业要求.txt》逐项核对系统当前达成情况。
> 结论口径：以满足**现场稳定演示**为目标，不追求成熟商业产品水平。
> 更新时间：2026-09-11（随验收进度更新）

## 1. 功能要求验收矩阵

| # | 大作业要求 | 系统实现（模块/提交依据） | 验收方式 | 结论 |
|---|---|---|---|---|
| 1 | 多智能体协作的软件安全审计系统 | LangGraph 编排 + 规划执行循环（T25 AgentLoop）；调度/导入解析/逆向分析/关键逻辑/静态审计/独立复核/模糊测试/验证/利用生成/报告等智能体（架构 §5，T27/T29/T30/T31 落地） | 任务页「智能体协作」面板 + agent-runs API 展示角色/决策/状态 | 已验证（AgentRun 轨迹落库并在前端可查，6 样本全程留痕） |
| 2 | 开源软件与二进制软件漏洞挖掘、利用验证的完整系统 | 源码管线（tree-sitter/Semgrep/cppcheck/语义审计）+ 二进制管线（DIE/UPX/Ghidra/angr）→ PAIR → Finding → 复核 → Proof/Exploit → 报告 | E2E：源码样本 + ELF 样本各一条全链路 | 已验证（4 主样本 + 2 加分样本全链路闭环） |
| 3 | 后端 Rust / 前端 TS+Svelte+ConnectRPC（可自选） | 自选 Python（FastAPI+LangGraph，逆向/模型生态成熟）+ Svelte 5 + TypeScript + WebSocket | 架构文档 §10 选型依据 | 满足（自选口径） |
| 4 | 代码库导入和模块解析 | 安全导入（路径穿越/归档炸弹防护）+ tree-sitter 索引 + CapabilityProfile（T12） | 源码样本导入 → PAIR 函数清单 | 已验证（py-eval-calculator 源码管线闭环） |
| 5 | 二进制逆向分析重点：智能体识别加壳/混淆特征、自主规划步骤、协同调用脱壳/反编译/解混淆工具、生成可读代码或伪代码 | DIE 壳指纹 + 控制流平坦化启发式（T27）；逆向规划智能体自主决定脱壳/反编译/解混淆/angr 定点目标并落 AgentRun 轨迹；UPX -d、Ghidra Headless、angr 反扁平化、模型可读伪代码派生工件（T26/T28） | 加壳/混淆样本演示：识别 → 脱壳 → 伪代码 → 可读化 | 已验证（UPX 识别+脱壳、平坦化混淆识别，伪代码审计命中） |
| 6 | 基于反编译可读代码的漏洞检测 | 语义审计智能体以伪代码为上下文审计，模型 finding 锚定二进制地址防幻觉（T29 二进制侧） | ELF 样本产出候选 Finding | 已验证（3 个 ELF 样本全部命中预期 CWE，锚定真实地址） |
| 7 | 语义理解、控制流及调用关系分析，自动标定认证、加解密、注册等关键逻辑，展示函数位置、关联代码、调用链及判定依据 | 关键逻辑标定（导入表/字符串特征 + 模型确认，T30）；函数工作台展示 caller/callee 联动、伪代码、判定依据（T33） | 教学样本标定认证/加解密函数并在前端展示 | 已验证（样本 1/2 key-logic 运行记录与 PAIR 属性落库） |
| 8 | 人工复核、标注修正、报告导出 | 人工复核与标注 API + 前端入口（T33）；Markdown/PDF/SARIF 报告（T21/T34） | 浏览器操作演示 | 已验证（浏览器全链路走通：登录→项目→任务→发现→报告下载） |
| 9 | 多个协作智能体：导入解析 / 缺陷审计（静态+模糊测试+智能体验证+独立复核过滤误报）/ 漏洞自动利用（利用代码+证据链）/ 报告生成 | 静态规则+语义审计候选 → 独立复核（隔离上下文）过滤误报；确认后自动投递 fuzz（T32）与 exploit（T31）；利用脚本经安全红线校验后在沙箱执行，Poc+Evidence 落证据链；报告含四要素 | E2E 证据 | 部分验证（静态审计/复核/报告/证据链闭环✓；fuzz 已派发但沙箱内执行失败、exploit 因无 confirmed 未触发——均为设计门禁或已记录缺陷，见 §11） |
| 10 | 对比其他开源项目验证创新性 | 见本文档第 6 节（与 DeepAudit 等对比） | 文档 | 已完成初稿 |

## 2. 测试要求验收矩阵

| # | 大作业测试要求 | 样例 | 来源与口径 | 结论 |
|---|---|---|---|---|
| 1 | 任意 2 种开源大模型软件的漏洞挖掘和利用验证 | （a）系统由 DeepSeek、智谱 GLM 两种大模型驱动完成审计（模型访问层 OpenAI 兼容，已双端点实测）；（b）作为被测对象的开源大模型软件样本（如 llama-cpp-python 0.2.71 CVE-2024-34359 源码）| 口径说明见第 5 节 | 待演示验证 |
| 2 | 任意 2 种具备加壳功能闭源软件的漏洞挖掘和利用验证 | 自建教学样本：vuln-stack-overflow（UPX 加壳 ELF）、vuln-cmd-injection（UPX 加壳 ELF）| 自构建（用户授权口径：找不到合适现成样本时自建）；分发形态=仅二进制 | 待演示验证 |
| 3 | 任意 2 种具备混淆功能闭源软件的漏洞挖掘和利用验证 | 自建教学样本：混淆 ELF ×2（控制流平坦化） | 同上 | 待演示验证 |

> 口径声明（对老师/答辩）：自建样本为自研无害教学程序，产出二进制后仅以二进制形态分发（源码仅留存于 `code/tests/fixtures` 用于登记溯源），具备"闭源+加壳/混淆"属性；UPX 为加壳器、控制流平坦化为混淆技术，均经 DIE 实测证据确认，不宣称使用商业保护器。

## 3. 完整链路连通情况

E2E 于 2026-09-11 在本 worktree 代码重建的完整栈上执行：`code/compose.yaml` 主栈 9 服务 + binary-tools（api/analysis-worker/dispatcher/web/orchestrator 均含本轮全部修复后重建为 `:dev` 镜像），检测模型全程 **deepseek-flash**（无切换），驱动脚本 `code/scripts/demo_e2e.py`。证据工件落 `code/docs/demo/artifacts/<task_id>/`；逐样本执行记录与缺陷清单见 `code/docs/demo/e2e-evidence.md`。

| 环节 | 连通状态 | 关键 Job 状态 | 产出工件引用 |
|---|---|---|---|
| 导入（上传/登记/幂等/格式校验） | 通 | 主线 4 样本 import 全部 succeeded（加分样本见证据文档） | 各任务 import 工件 |
| 解析（源码/二进制归一化） | 通 | binary-analysis-result=complete（混淆样本 31 函数/26 段伪代码/72 基本块）；source_analysis succeeded | PAIR 函数清单；UPX 脱壳派生工件（upx-unpacked） |
| 逆向（DIE/UPX/Ghidra，沙箱隔离执行） | 通 | tool_runs（binary-header/upx/binary-facts）全部 succeeded | DIE 壳指纹、Ghidra 伪代码（经沙箱执行） |
| 静态语义审计（模型） | 通 | semantic_audit succeeded | 候选 Finding（锚定 PAIR 函数/二进制地址） |
| 独立复核（隔离上下文） | 通 | review Job 全部 succeeded（源码样本 review×3） | 复核结论（含 unverifiable 有据降级） |
| 关键逻辑标定 | 通 | key-logic AgentRun（失败自动重试后成功） | AgentRun 决策轨迹（agent-runs API 可查） |
| 动态（fuzz/exploit） | 部分通 | fuzz 派发后真实进入沙箱执行，执行阶段以结构化失败收敛（3 类失败码落账）；exploit 未触发（无 confirmed，设计门禁） | 结构化失败原因可追溯（stdout/stderr 留存 CAS） |
| 报告（中文 md/pdf） | 通 | 每样本 markdown+pdf 双 Job succeeded（6 样本共 12 份，重复请求幂等复用） | `code/docs/demo/artifacts/*/report.md`、`report.pdf` |
| 人工复核/标注/重试运营面 | 通 | retry 端点 404/409 探针通过（200 正向探针完整结论见证据文档） | Outbox `job.requested` 新事件、worker 认领重执行 |

任务级端到端耗时（创建→completed）：packed-overflow-note 130s；obfuscated-heap-overflow 1189s（含缺陷 6ccaf36 暴露与修复期间的重执行）；py-eval-calculator 306s；benign-checksum 220s。全部任务 completed，状态迁移 created→validating→analyzing→reviewing→completed。

## 4. 实际发现并验证漏洞的证据

| 样本 | 任务 ID | Finding（CWE / 等级 / 位置） | 复核与验证 | 证据工件 |
|---|---|---|---|---|
| packed-overflow-note（UPX 加壳） | `task:d894a0baab534b12b18a82f65aeb42c3` | CWE-120，high，save_note@0x401157（strcpy 无界拷入定长缓冲） | 复核判 unverifiable（有据降级，模型解释不单独 confirm）；与样本 README 预期漏洞的函数/CWE/机制完全一致——**命中** | `artifacts/task_d894a0ba…/`（中文报告 md+pdf、findings.json、jobs.json） |
| obfuscated-heap-overflow（平坦化+不透明谓词+XOR 字符串） | `task:553aea2437944db18713103131574104` | CWE-120（预期 CWE-122 的父类），high，parse_token（strcpy 拷入 malloc(1200) 定长堆缓冲） | 漏洞本体（函数+机制）命中，CWE 粒度偏差如实记录；fuzz 已自动派发并真实进入沙箱执行 | `artifacts/task_553aea24…/` |
| py-eval-calculator（Python 源码） | `task:b6d0bce331bc4596abc985e37e2b16c7` | CWE-95 ×3：critical（eval 直接执行用户输入表达式）、high（main() 命令行参数转发至 eval）、high（静态规则候选）——静态规则+语义审计双通道 | review×3 succeeded；每个发现自动派发 fuzz（3 Job，执行阶段结构化失败落账） | `artifacts/task_b6d0bce3…/` |
| benign-checksum（无害对照） | `task:fc06d4093fe1447ea60b51b83be79e05` | 无高危/内存破坏类误报；仅 1× CWE-252 low（错误处理健壮性提示） | 复核未将 low 提示降为 false_positive → result=partial，作为误报度量如实呈现 | `artifacts/task_fc06d409…/` |
| packed-command-injection（UPX 加壳，加分样本） | 见证据文档 | CWE-78，run_report（命令注入） | **命中** | 见证据文档加分样本小节 |
| obfuscated-format-string（平坦化混淆，加分样本） | 见证据文档 | CWE-134，audit_log（格式化字符串）；另报出额外 CWE-287 认证类候选 | **命中**（额外候选如实标记） | 见证据文档加分样本小节 |

要点：

- 全部 6 个任务状态 **completed**，每样本中文报告（Markdown+PDF）双产出。
- 确认门禁按设计生效：本轮无 `confirmed` 发现，exploit Job 未触发（FindingPolicy 强证据门禁——模型解释不能单独 confirmed）；样本 1 的独立复核在无代码上下文时判 unverifiable，正是该门禁的活体证据。
- 伪代码上下文修复（`634b9ab`）前后对照：同一模型修复前明确回答"无代码可审"，修复后产出带函数定位的具体发现。

## 5. 口径与限制说明

### 5.1 "开源大模型软件"的两种口径
- 口径 A（系统引擎）：DeepSeek、智谱 GLM 已通过统一模型访问层接入并可切换，满足"支持对任意 2 种开源大模型软件"的系统侧要求。
- 口径 B（被测对象）：题面也可读作"以开源大模型软件为被测对象挖掘漏洞"。系统首版语言范围（C/C++/Python/Java）可覆盖 Python 系开源大模型软件（llama-cpp-python、LMDeploy）；Ollama（Go）不在首版范围。演示以口径 B 的 llama-cpp-python 源码审计为增强项。

### 5.2 闭源样本口径
严格口径的"自身具备加壳功能的商业闭源软件"（Themida/WinLicense）存在旧版合法获取与老系统复现的高风险，按用户指示采用自建样本路线（见第 2 节声明）。

### 5.3 自动化利用分级
按调研报告 §3.3：L0 候选 / L1 安全证明（marker、受控崩溃、拒绝响应）/ L2 受控骨架 / L3 武器化利用（不纳入）。系统自动生成的是 L1 级最小安全证明脚本。

## 6. 与其他开源项目对比及创新点

> 完整对比报告见 `code/docs/demo/与开源项目对比及创新点.md`（对比对象：DeepAudit、Semgrep、Ghidra、PentestGPT）。

| 维度 | DeepAudit（主参考） | VulnWeaver（本系统） |
|---|---|---|
| 定位 | 源码 AI 审计（49 CVE 由其闭源版产出，开源代码核实） | 源码 + 二进制全链路漏洞挖掘与利用验证 |
| 二进制逆向 | 不支持（依赖清单无 Ghidra/angr，开源代码核实） | DIE/UPX/Ghidra/angr + 逆向规划智能体 + 解混淆 + 可读伪代码（原始件不可变，派生可追溯） |
| 智能体编排 | LangGraph 四智能体 + 检查点（开源代码核实） | 10 类智能体共享规划—执行—观察循环；结构化 ActionPlan 经 ToolRegistry/PolicyEngine 校验；AgentRun 决策轨迹 API 可查 |
| 动态验证隔离 | Docker 沙箱 PoC 验证（禁网/能力丢弃），但后端容器直接挂载 Docker Socket（其 compose 注释"沙箱执行必须"），无模糊测试管线 | Sandbox Runner 唯一容器运行时入口（控制面/普通 Worker 不挂 Docker Socket）；AFL++ 模糊测试 + 崩溃分诊 + 最小复现 + 利用验证 |
| 误报过滤 | RAG 语义增强 + PoC 验证自我修正 | 静态审计与独立复核上下文隔离；finding 锚定 PAIR 防幻觉；模型解释不能单独 confirm（FindingPolicy 强证据门禁） |
| 报告 | PDF/Markdown/JSON | Markdown/PDF/SARIF（codeFlows 调用路径）+ 人工复核历史 + 任务回放 |

创新点归纳（详见完整报告，每条均落到模块/ADR/任务包）：
1. 源码与二进制统一 PAIR 中间表示，伪代码审计候选锚定真实地址防模型幻觉。
2. 逆向步骤（是否脱壳/解混淆/定点分析）由模型自主规划且全程留痕可回放。
3. 证据驱动的确认门禁：`confirmed` 必须满足 FindingPolicy 强证据要求，推测内容在报告中显式标记。
4. 控制面/执行面分离的智能体工具治理：结构化 ActionPlan + 策略引擎，智能体不直接执行命令，Sandbox Runner 是唯一运行时入口。
5. Finding→模糊测试→自动利用全自动闭环：脚本经安全红线校验后在隔离沙箱执行，证据链落库。

## 7. 测试样例清单、构建方法、预期与实际结果

样例全部为自编无害教学程序，存放于 `code/tests/fixtures/`（`README.md` 登记来源与授权依据，sha256 清单随提交 `84b467a`），构建脚本 `code/tests/fixtures/build-all.sh`。加壳器=UPX、混淆=控制流平坦化+不透明谓词（+XOR 字符串/格式化字符串变体），均经 DIE 实测证据确认；分发形态仅二进制（闭源口径见第 5.2 节声明）。

| # | 样本 | 形态 | 构建方法 | 预期结果 | 实际结果 | 结论 |
|---|---|---|---|---|---|---|
| 1 | packed-overflow-note | UPX 加壳 ELF（核心演示） | 自编 save_note（strcpy→定长栈缓冲）教学程序，编译后 UPX 加壳 | CWE-120 save_note 栈溢出 | CWE-120 high，锚定 save_note@0x401157 | 命中 |
| 2 | obfuscated-heap-overflow | 平坦化+不透明谓词+XOR 字符串 ELF | 自编 parse_token（strcpy→malloc(1200) 堆溢出）教学程序，平坦化混淆编译 | CWE-122 parse_token 堆溢出 | CWE-120（父类）high，parse_token，函数+机制一致 | 命中（CWE 粒度偏差，如实记录） |
| 3 | py-eval-calculator | Python 源码 | 自编 eval 计算器（evaluate() 直接 eval 用户输入） | CWE-95 | CWE-95 ×3（critical+high×2，静态+语义审计双通道） | 命中 |
| 4 | benign-checksum | Python 无害对照 | 自编文件校验工具，无注入/内存安全问题 | 无发现（或基线说明） | 无高危误报；1× CWE-252 low 健壮性提示，result=partial | 对照达标（无高危误报；低危提示未被复核压制，如实记录） |
| 5 | packed-command-injection | UPX 加壳 ELF（加分） | 同 #1 方法，run_report 经 system 执行外部输入 | CWE-78 | CWE-78，run_report | 命中（加分） |
| 6 | obfuscated-format-string | 平坦化混淆 ELF（加分） | 同 #2 方法，audit_log 含格式化字符串缺陷 | CWE-134 | CWE-134，audit_log；另报额外 CWE-287 候选 | 命中（加分） |

样本矩阵覆盖「源码/二进制 × 有害/无害 × 加壳/混淆」对照维度；样本 1/2 额外留存了「修复前 no_findings → 修复后命中」的缺陷定位回归记录（见证据文档缺陷清单），可作为审计上下文完整性重要性的答辩素材。

## 8. 自动化测试与端到端测试结果

**合并树（feat/demo-final = origin/main T38-T40 + 本地提交）质量门禁：**

- pytest：**543 passed / 5 skipped**（5 项为需 live Sandbox Runner/Docker 的显式 opt-in；PostgreSQL/Redis 集成实跑）
- ruff 全部通过；pyright 0 错误；svelte-check 0 错误 0 警告
- 合并前基线：512 passed，分支覆盖率 **82.60%**（总体门槛 80%）

**链路修复定向回归**（缺陷定位与验证明细见证据文档）：

| 修复 | 回归范围 | 结果 |
|---|---|---|
| `6d5a5a2` 脱壳件传沙箱 | tests/binary_analysis | 35 passed |
| `981a1a6` 报告幂等复用 | tests/api | 35 passed |
| `634b9ab` 语义审计伪代码正文 | tests/orchestrator + tests/binary_analysis | 108 passed |
| `6ccaf36` fuzz Job kind 迁移（0020） | tests/persistence | 35 passed |
| `4326d52` retry 端点 | tests/api + tests/persistence + tests/orchestrator | 143 passed |

**端到端结果**：6 样本全部 completed，命中矩阵见第 7 节；中文报告每样本 Markdown+PDF 双产出（共 12 份）；retry 端点 404/409 探针通过（200 正向探针完整结论见证据文档）。

## 9. 前端设计说明与关键页面截图

### 9.1 设计目标与信息架构（总控 astra 设计）

任务详情页从"数据列表堆叠"重构为**审计指挥中心**：打开任务即能看到审计全过程的状态、进度与协作情况。设计保留既有设计系统（暗色底 + 青柠唯一强调色、面板 12px/控件 8px 圆角、Segoe UI/微软雅黑字体栈），未引入新依赖与图标库。

页面结构（自上而下）：

1. **页头**：面包屑、任务名、中文状态徽章、操作区（取消任务 / 重试失败作业 / 生成 Markdown·PDF·SARIF 报告）。
2. **审计阶段流水线**（核心新增，全宽）：固定 8 阶段
   `样本导入 → 结构解析 → 逆向还原(源码任务显示"静态扫描") → 语义审计 → 独立复核 → 模糊测试 → 验证与利用 → 报告生成`。
   每阶段按其 JobKind 集合聚合真实 Job 状态，展示：中文阶段名、状态（等待/运行中/完成/部分完成/失败/已取消/未启用）、Job 计数（如 3/4）、耗时、失败原因中文摘要与「重试」按钮。当前运行阶段青柠高亮+脉冲；窄屏纵向排列。底部"N/M 阶段完成"总进度条。
3. **指标条**：漏洞总数、已确认数、失败作业数、报告状态。
4. **漏洞统计**：严重等级横向条形（严重/高危/中危/低危/提示）+ 确认状态计数徽章，随 WebSocket 实时更新。
5. **双栏主区**（≥1180px）：左=漏洞列表+漏洞详情（证据链、Poc/利用入口、人工复核、标注，功能全部保留）；右=Tab「智能体协作 | 事件流 | 作业列表」。
   - 智能体协作：按 agent-runs 渲染角色卡片（调度/导入解析/逆向分析/关键逻辑/静态审计/独立复核/模糊测试/验证/利用生成/报告），显示模型、决策数、token、耗时、状态，运行中脉冲。
   - 事件流：事件分类着色（阶段=青柠、作业=琥珀、发现=红、错误=深红、系统=灰），标题中文化、载荷原文可展开、类型过滤 chips、自动滚动开关。
   - 作业列表：保留原有重试入口。
6. **函数与调用链工作台**：保留原有 caller/callee 联动、伪代码、关键逻辑标定展示。

### 9.2 中文化

全部枚举（作业类型/状态、任务状态、发现状态/类别/严重等级、证据类型/强度、Poc 结果等）集中映射于 `apps/web/src/lib/i18n.ts`；约 90 条失败码映射为中文（未命中显示"错误码：原文"）；界面残留英文文案全部中文化；日志载荷、代码、路径、地址保留原文。

### 9.3 关键页面截图

截图文件位于 `code/docs/demo/screenshots/`（1280×720，合并后真实运行栈实测截取）：

| 文件 | 内容 |
|---|---|
| `task-overview.png` | 任务总览（最新前端）：「审计过程」7 阶段面板（导入与逆向/分析基线/智能体审计/独立复核/模糊测试/漏洞验证/报告生成）、执行单元进度 5/5、重新审计与查看报告入口 |
| `findings-and-report-center.png` | 智能体审计卡片（模型/状态/作业号）与函数调用链工作台 |
| `finding-detail.png` | 漏洞条目与详情（CWE-120 · 高危 · 置信度 40%）、报告导出入口 |
| `finding-evidence.png` | 复核历史（模型原文结论）、证据链明细、Markdown/PDF 报告下载 |
| `workbench-pseudocode.png` | save_note 反编译伪代码与调用方/被调用方联动 |
| `agent-trajectory.png` | 智能体运行轨迹：三次复核模型调用（决策数、token、耗时、失败码）、事件流 LIVE |

> 说明：远端 PR #61（web-audit-integration）已实现审计过程可视化与中文视图；我方"审计指挥中心"组件（阶段流水线/智能体面板/全站 i18n）保留在 `apps/web/src/lib/` 下作为备选实现（设计规格见 §9.1）。
