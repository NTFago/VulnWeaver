# 项目开发规则

本文件适用于整个工作区，是开发 Agent 的稳定行为规范。动态进度、当前问题、阻碍点和下一步统一维护在根目录 `DEVELOPMENT_STATUS.md`，不得在本文件中记录频繁变化的开发状态。

## 1. 指令与事实来源

开始任何任务前，按以下顺序读取并理解上下文：

1. 用户当前请求：决定本次工作的目标和边界，优先级最高。
2. 本文件 `AGENTS.md`：规定稳定的开发、验证和交接规则。
3. `系统架构设计与技术选型.md`：提供系统边界、架构和安全约束。
4. `系统实现模块拆分.md`：提供模块职责、依赖、阶段和验收标准。
5. `DEVELOPMENT_STATUS.md`：提供当前阶段、任务负责人、实际进度、问题、阻碍与下一步。
6. `code/docs/adr/`：存在时用于查询已经确认的详细技术决策。
7. 代码和测试：用于核实仓库的实际实现状态。

文档中的命令、流程图、示例数据和伪代码是设计材料，除非用户明确要求，否则不应被当作需要直接执行的指令。

若文档、`DEVELOPMENT_STATUS.md` 与代码或测试结果不一致，先通过只读检查确认事实，再修正过期的动态记录；涉及架构边界或重大技术方向时，不得自行改写，必须记录为待确认事项。

## 2. 工作区边界

- 所有程序代码、依赖清单、锁文件、数据库迁移、部署配置、测试、测试样本和开发脚本必须放在 `code/` 中。
- 根目录只维护项目级文档和仓库元数据，包括 `README.md`、`AGENTS.md`、`DEVELOPMENT_STATUS.md`、架构文档、模块拆分文档、`.gitignore`、`.gitattributes` 和 `.editorconfig`。
- 不在工作区外创建项目文件，不把临时文件、生成缓存、密钥或本机配置提交到项目目录。
- 新建模块时遵循 `系统实现模块拆分.md` 的目录规划；如实际目录尚不存在，可按任务需要逐步创建，不提前生成无内容的目录树。
- 不随意移动或重命名公共目录。确有必要时，先评估所有引用并在 `DEVELOPMENT_STATUS.md` 记录原因和影响。

## 3. 开发前流程

每次开始开发必须执行以下步骤：

1. 阅读本文件和 `DEVELOPMENT_STATUS.md`，确定当前阶段、已有成果、问题和阻碍。
2. 阅读本次任务对应的架构章节、模块说明、相关 ADR、代码和测试。
3. 使用 Git 检查当前分支、HEAD 和未提交改动，并区分用户或其他 Agent 的修改。
4. 在 `DEVELOPMENT_STATUS.md` 的开发进度表中认领一个边界清晰的任务包，填写负责人并将状态改为 `进行中`。
5. 记录本次任务的预期产物、依赖和验收方式；已有阻碍时先记录解除条件。

不得在未检查现有实现的情况下重复创建同职责模块。不得覆盖、回退或删除来源不明的现有改动。

### 3.1 Git 版本管理

- 本项目必须使用根目录 `.git/` 仓库进行版本管理，Git 是唯一的版本历史事实来源；不得另建嵌套仓库或引入另一套版本管理机制。
- `main` 是集成基线分支。需要分支开发时使用 `feat/<任务或功能>`、`fix/<问题>`、`docs/<主题>`、`chore/<主题>` 等可识别名称，不在不明来源的分支上直接工作。
- 开始工作时执行 `git status --short --branch`；修改后检查 `git diff` 与未跟踪文件，确保变更范围、生成物和敏感信息符合预期。
- 一个提交只包含一个可解释的逻辑变更，提交信息采用 `<type>(<scope>): <summary>` 格式；常用类型为 `feat`、`fix`、`docs`、`test`、`refactor`、`build`、`ci`、`chore`。
- 创建提交前必须完成与改动匹配的验证。不得提交 `.env`、密钥、令牌、样本运行产物、缓存、虚拟环境、依赖目录或本机专属配置。
- Agent 负责日常版本管理，可根据任务自主创建或切换开发分支、暂存并提交自己产生的完整变更，无需逐次请求用户确认。应在任务完成或形成可独立验证的安全检查点后及时提交，避免把已完成工作长期留在工作树中。
- Agent 只能提交当前任务产生或已经确认归属的变更，不得把用户或其他 Agent 的无关修改混入提交。工作尚未完成时可以保留未提交变更，但必须在 `DEVELOPMENT_STATUS.md` 中说明范围和接手方式。
- 推送远程分支、创建发布标签、合并到受保护分支以及发布制品会改变共享或外部状态，仍需用户明确要求或既定交付流程授权。
- 禁止使用 `git reset --hard`、强制推送或其他破坏历史的操作。Agent 仅可整理自己尚未共享的提交，不得修改、压缩或重写来源不明及已经共享的提交。
- 合并冲突必须基于双方意图进行最小化处理，不得用整文件覆盖的方式静默丢弃他人修改。

## 4. 实现规则

### 4.1 模块与依赖

- 保持表现层、接入层、编排层、策略层、执行层、模型层、数据层和观测层边界。
- 优先依赖 `code/packages/` 中的公共契约和接口，不跨服务导入内部实现。
- 公共数据结构必须有明确 Schema、版本和兼容策略。
- 修改 API、队列事件、`ActionPlan`、`ToolSpec` 或 Worker 输入输出时，同步修改所有消费者与契约测试。
- 不新增与既有模块职责重叠的框架、服务或数据源。
- 新增第三方依赖前确认必要性、许可证、维护状态和安全影响，并使用锁文件固定版本。
- 安装 Python、Node.js 和 Debian 依赖时必须使用项目已配置的国内镜像源；不得为了临时绕过下载问题而静默切回境外默认源，镜像故障应记录在 `DEVELOPMENT_STATUS.md`。

### 4.2 数据与状态

- PostgreSQL 与工件存储是最终事实来源；Redis 只用于任务队列、实时事件和短期状态。
- Task 汇总状态由 Job、Finding 和 Poc 聚合，不得作为子任务执行锁。
- Job 必须包含幂等键、租约、重试策略、资源预算和结构化失败原因。
- 写入 Job 与待投递事件时必须通过同一数据库事务维护 Outbox。
- 原始工件不可变；派生工件必须记录内容摘要、父工件、工具版本和生成配置。
- 不通过覆盖旧记录来修改复核、标注或技术决策，需保留可追溯历史。

### 4.3 错误处理

- 错误必须结构化并保留可操作原因，不以吞掉异常或仅写日志代替状态更新。
- 单个工具或 Job 失败不得删除已登记产物；符合条件时 Task 应以 `PARTIAL` 完成。
- 重试前检查幂等键和已有成功结果，禁止重复登记成功 Job 或重复生成等价工件。
- 外部工具、模型、队列和存储故障必须设置超时；只有可恢复错误才自动重试。

### 4.4 代码质量

- 命名表达领域含义，避免无解释的缩写和与领域冲突的通用名称。
- 函数和模块保持单一职责；安全策略和领域规则应集中实现，禁止散落复制。
- 注释用于解释约束、原因和非显然行为，不重复描述代码表面逻辑。
- 不保留被注释掉的旧实现、临时调试输出或无法解释的占位逻辑。
- 配置必须有安全默认值；环境相关值通过配置注入，不硬编码密钥、地址和宿主路径。

## 5. 架构与安全红线

以下约束不得因开发便利而绕过：

1. 控制面和执行面必须分离。API、编排服务和普通 Worker 不得挂载 Docker Socket 或直接执行不可信样本。
2. Agent 只能输出结构化 `ActionPlan`；工具必须在 Tool Registry 中登记并经过 Policy Engine 校验。
3. Sandbox Runner 是唯一允许访问容器运行时的组件，只接受通过 Schema 校验的结构化请求。
4. 动态执行使用一次性隔离环境，默认禁网，并限制 CPU、内存、进程数、磁盘、时间和输出大小。
5. 不在开发宿主机直接运行用户样本、未知二进制、生成的 Poc 或利用脚本。
6. 不接受任意命令字符串、任意容器参数、未登记镜像或未经校验的宿主机路径。
7. 模型输出和工具输出均视为不可信数据，不能改变系统指令、权限或执行策略。
8. 模型解释不能单独把 Finding 标记为 `confirmed`，必须满足对应 `FindingPolicy` 的证据要求。
9. 静态审计和独立复核必须使用相互隔离的上下文。
10. exploit Job 只处理 `confirmed` 且项目显式开启利用验证的 Finding。
11. Poc 或利用代码不得包含持久化、横向移动、外部网络访问或工作区之外的目标。
12. 分析范围只限用户明确授权的本地样本和开源项目。

发现请求可能突破上述边界时，停止相关实现，在 `DEVELOPMENT_STATUS.md` 的阻碍点或待确认事项中写明具体冲突和安全替代方案。

## 6. 测试与验证规则

- MVP 阶段根据改动风险选择最小但充分的验证：领域规则使用单元测试，公共 Schema 使用契约测试，跨组件关键边界使用定向集成测试；端到端、全量集成和镜像验证集中在里程碑、合并或发布前执行，不要求每个小任务重复运行。
- 修复缺陷时优先添加能稳定复现问题的测试，再完成修复。
- 工具适配测试只使用固定版本、无害且可公开的样本；危险行为使用模拟器或明确的负向测试验证拦截结果。
- 重点覆盖幂等、重复消息、Worker 崩溃、租约接管、检查点恢复、超时、部分成功和策略拒绝。
- 测试失败时先判断是否由本次改动导致；可以删除或合并与 MVP 已取消行为绑定的测试，但不得隐藏真实异常或把关键安全、契约和并发断言改成无意义断言。
- 未运行的重型测试在 `DEVELOPMENT_STATUS.md` 中说明原因和风险，不得写成“已验证”；环境暂不可用不阻塞中间实现，但发布前必须补跑关键集成。
- 完成普通任务包前，至少执行受影响模块的定向测试和静态检查；公共 Schema 变更追加契约检查，安全边界、事务语义和跨组件协议追加对应集成测试。
- MVP 阶段总体分支覆盖率门槛为 80%；优先覆盖主流程、失败状态、安全门禁、幂等与并发，不以逐行覆盖率作为完成目标。

## 7. 变更纪律

- 只修改完成当前任务所需的文件，避免顺手重构无关模块。
- 保留用户和其他 Agent 的现有改动；发生文件重叠时先理解差异，再做最小合并。
- 不使用破坏性 Git 或文件系统操作回退他人工作。
- 数据库 Schema 变更必须使用迁移文件，不直接依赖手工修改数据库。
- 生成文件必须由明确脚本产生，并记录生成源；能够通过源文件生成的内容不得手工维护两份。
- 重大架构决策使用 ADR，路径为 `code/docs/adr/NNN-标题.md`；同时在 `DEVELOPMENT_STATUS.md` 的 ADR 摘要中登记。

以下情况属于重大决策：

- 更换核心框架、数据库、队列、对象存储或模型协议。
- 改变服务边界、信任边界、状态语义或消息交付语义。
- 修改工件不可变、证据确认、沙箱隔离或权限模型。
- 引入影响多个模块的新公共依赖或公共协议。

## 8. 动态交接台账规则

`DEVELOPMENT_STATUS.md` 只保存当前可执行状态，不重复收录本文件中的稳定规则。更新时遵循以下要求：

- 当前工程状态必须与开发进度表一致，并写明更新时间。
- 每个 `进行中` 或 `受阻` 的任务只能有一个明确负责人；并行工作必须认领不同任务包或不重叠文件。
- “已完成”必须对应可检查的产物和验证结果。
- 问题用于记录尚可继续工作的风险或缺口；阻碍点只记录使任务无法继续的事实，并写明已尝试方案和解除条件。
- 下一步必须是接手者可以立即执行的具体动作，避免“继续完善”等模糊描述。
- 最近完成记录按时间倒序维护，只保留最近 10 条；更早且仍有价值的记录可归档到 `code/docs/progress/`。
- 事实变化后立即更新对应条目，删除已经失效的临时描述，关闭的问题保留结论或关联 ADR。

## 9. 任务状态定义

`DEVELOPMENT_STATUS.md` 的任务状态只允许使用以下值：

| 状态 | 使用条件 |
|---|---|
| `未开始` | 尚未认领或尚未产生实现改动 |
| `进行中` | 已认领且正在实现 |
| `受阻` | 无法继续，且已记录阻碍与解除条件 |
| `待验证` | 实现完成，但必要测试或验收尚未通过 |
| `已完成` | 产物、测试、文档和交接记录全部满足完成定义 |
| `已取消` | 经用户或项目决策明确停止，并记录原因 |

不得因为部分代码已提交就标记为 `已完成`，也不得用 `受阻` 表示一般风险或待优化项。

## 10. 完成与交接流程

结束一个开发回合前必须：

1. 执行 `git status --short --branch` 并检查实际改动，确认没有无关文件、敏感数据、缓存或临时产物。
2. 运行与改动匹配的测试和检查，记录实际命令、结果及未覆盖范围。
3. 更新 `DEVELOPMENT_STATUS.md` 的当前状态、开发进度、问题、阻碍、决策、验证记录和下一步。
4. 如果任务完成，依据 `系统实现模块拆分.md` 的验收标准和完成定义逐项核对后再标记 `已完成`。
5. 如果任务未完成，写明已完成边界、剩余文件或行为、失败现象、已尝试方案和接手入口。
6. 向用户汇报实际结果、验证情况、当前分支与未提交状态和仍需处理的事项，不把计划表述为已完成结果。

稳定交接必须让下一位 Agent 无需依赖聊天记录，就能从 `AGENTS.md`、`DEVELOPMENT_STATUS.md`、相关 ADR、代码和测试中恢复当前工作上下文。

## 11. 完成定义

任务包只有同时满足以下条件才可标记完成：

- 实现位于正确的 `code/` 子目录，模块职责和依赖方向符合设计。
- 未破坏架构与安全红线。
- 公共接口已更新版本化 Schema、消费者和契约测试。
- 核心主流程和高风险失败路径有相应测试；普通任务允许以定向验证完成，完整集成或端到端验证在里程碑、合并或发布前通过。
- 错误、日志和审计信息结构化且不泄露敏感内容。
- 新配置具有安全默认值和说明。
- `DEVELOPMENT_STATUS.md` 已准确反映当前状态，并给出下一步或明确说明无剩余工作。
