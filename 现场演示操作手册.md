# VulnWeaver 现场演示操作手册

> 面向现场答辩/演示场景，按"稳定演示优先"编排。全部操作在演示机上完成；
> 样本只经系统上传并进入隔离沙箱，演示机不直接运行任何样本。
> 状态：初稿（端到端实测后更新等待时间与话术）

## 1. 演示前准备（一次性，约 10 分钟）

1. **启动栈**（Docker Desktop 需已启动）：
   ```bash
   cd code
   docker compose up -d          # 9 个服务：web/api/orchestrator/dispatcher/analysis-worker/sandbox-runner/postgres/redis(+proof 按需)
   docker compose ps             # 确认全部 Up
   ```
2. **打开工作台**：浏览器访问 `http://127.0.0.1:8080/`。
   - 全新数据库：页面会引导注册唯一管理员（一次性）。
   - 已有数据库：使用 `owner` 账号登录（密码见 `code/secrets/personal_password.txt`）。
3. **确认模型配置**：设置页 → 模型档位（复核/规划/审计）。可配 DeepSeek 或智谱 GLM 任一/多个（OpenAI 兼容端点）。若数据库已有历史配置，确认「连接测试」可用即可。
4. **确认工具镜像**：设置页应显示二进制工具/AFL/Proof 镜像摘要已解析（本地镜像存在即可）。
5. **待演示样本**（`code/tests/fixtures/`，演示前一夜跑一遍构建脚本确保产物最新）：
   - 加壳：`packed-overflow-note`（UPX+CWE-120）、`packed-command-injection`（UPX+CWE-78）
   - 混淆：`obfuscated-format-string`（平坦化+CWE-134）、`obfuscated-heap-overflow`（平坦化+不透明谓词+XOR 字符串+CWE-122）
   - 源码：`py-eval-calculator`（CWE-95）
   - 对照：`benign-checksum`（无漏洞，NO_FINDINGS 路径）

## 2. 主线演示脚本（约 8-10 分钟）

### 2.1 加壳闭源软件全链路（主打，约 4 分钟）
1. 新建项目「演示-加壳样本」，**勾选利用验证开启**。
2. 上传 `packed-overflow-note`（仅一个 5KB 的二进制，无源码——强调"闭源"形态）。
3. 创建分析任务 → 停留在任务页，指着**审计阶段流水线**讲解：
   - 样本导入 → 结构解析（DIE 识别 UPX 壳）→ 逆向还原（upx -d 脱壳 + Ghidra 反编译）→ 语义审计（大模型审计伪代码）→ 独立复核（第二模型独立复核过滤误报）→ 模糊测试/验证与利用（若开启）→ 报告生成。
   - 强调：阶段状态实时变化（等待/运行中/完成），逆向规划智能体的决策（是否脱壳、反编译哪些函数、是否解混淆）记录在「智能体协作」面板。
4. 漏洞出现后：点开 Finding 详情——严重等级、可信度、受影响位置（二进制地址+函数）、证据链、复核结论。
5. 切到「函数与调用链」工作台：点击函数看 caller/callee 联动与伪代码。
6. 生成中文报告（Markdown/PDF）→ 打开展示：执行摘要、风险统计、证据链、修复建议、"模型推断需人工确认"标记。

### 2.2 混淆闭源软件（约 2 分钟）
1. 上传 `obfuscated-heap-overflow`，建任务。
2. 讲解点：混淆特征识别（控制流平坦化/不透明谓词/字符串加密）→ 可读伪代码派生工件（原始反编译不可变，可读版本单独保存）→ 漏洞仍然命中。
3. 对比展示原始伪代码 vs 可读伪代码（如时间紧可在报告/工件引用里带过）。

### 2.3 源码管线（约 1 分钟）
- 上传 `py-eval-calculator`，展示源码导入 → tree-sitter 解析 → 静态+语义审计 → 候选发现，证明源码/二进制双管线统一。

### 2.4 无漏洞对照（约 1 分钟）
- 上传 `benign-checksum`：任务正常完成、无发现，证明系统不靠"永远报漏洞"凑数（NO_FINDINGS 有基线完整性门禁）。

## 3. 备选/加分演示（时间富余时）
- **动态验证**：对 `packed-command-injection`（CWE-78）的 confirmed Finding 查看自动生成的 Proof/利用脚本与沙箱执行记录（禁网、非 root、资源限额、可回放）。
- **模糊测试**：查看 fuzz Job 的覆盖率/崩溃簇证据（若配置了 AFL 镜像）。
- **人工复核**：现场对某个候选漏洞提交人工复核/标注，展示历史可追溯。
- **双模型切换**：设置页把审计档位从 DeepSeek 切到智谱 GLM 再跑一个样本，证明模型可替换。

## 4. 风险与预案

| 风险 | 预案 |
|---|---|
| 模型 API 超时/限流 | 任务会结构化降级（固定管线继续，语义审计失败码可见）；提前在演示晨跑通一次；备好第二个模型端点 |
| 动态链路（fuzz/proof）不可用 | 主线演示不依赖动态链路；找不到漏洞验证时如实展示"结构化失败原因"，本身是安全设计亮点 |
| 分析耗时超预期 | 样本都是 KB 级，单任务通常 1-3 分钟；演示前预跑一轮，答辩时用预跑结果讲解、现场再跑一轮佐证 |
| 浏览器 WS 断线 | 前端按最后事件序号自动恢复，不重复事件；刷新页面即可 |
| 现场无外网 | 模型调用需要外网；完全离线时改讲预先生成的报告与任务轨迹（提前缓存） |

## 5. 演示纪律（安全口径）
- 所有样本为自编无害教学样本（来源与授权见 `code/tests/fixtures/README.md`）。
- 动态执行只发生在一次性沙箱容器：禁网、非 root、只读根、资源限额。
- 不演示、不生成真实攻击载荷；利用验证输出为"最小安全证明"（崩溃/marker/拒绝）。
